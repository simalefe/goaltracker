# ---- Prod Profile ----
spring:
  jpa:
    show-sql: false
  h2:
    console:
      enabled: false
  thymeleaf:
    cache: true

logging:
  level:
    root: WARN
    com.goaltracker: WARN
    org.hibernate.SQL: WARN

